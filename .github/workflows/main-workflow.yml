name: Build and Run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: g++ -o test_program main.c WiringReferenceManual.c test_WiringReferenceManual.cpp
      
    - name: run
      run: ./testchecker
